# =============================================================================
# SciNet++ (2026) dependencies
# -----------------------------------------------------------------------------
# Modern re-implementation of "Discovering Physical Concepts with Neural
# Networks" (Iten et al., 2018) using current, maintained components:
#   V-JEPA (latent prediction) + PySR (AI-Feynman symbolic regression) +
#   physics probe (SciNet concept read-out) + AdaJEPA (test-time adaptation).
# Install:  pip install -r requirements.txt
# =============================================================================

# --- Core deep learning (V-JEPA world model) --------------------------------
torch>=2.2
tensorboard>=2.15

# --- Scientific stack (data generation, metrics, PCA) -----------------------
numpy>=1.24
scipy>=1.10            # double-pendulum ODE integration (solve_ivp)
scikit-learn>=1.3      # PCA for latent-space visualisation
matplotlib>=3.7

# --- Utilities ---------------------------------------------------------------
tqdm>=4.66
pyyaml>=6.0

# --- Symbolic regression (AI Feynman) ---------------------------------------
# PySR is Julia-backed: on first use it downloads a self-contained Julia
# toolchain automatically (a few minutes, one time). If you cannot install it,
# run the rest of the pipeline with:  python run_all.py --skip-symbolic
pysr>=0.17
